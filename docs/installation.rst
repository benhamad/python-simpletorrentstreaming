============
Installation
============

At the command line::

    $ easy_install SimpleTorrentStreaming

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv SimpleTorrentStreaming
    $ pip install SimpleTorrentStreaming
